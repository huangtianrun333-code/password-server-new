{
  "name": "password-server-new",
  "version": "1.0.0",
  "description": "全新的跨设备密码同步服务器",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "node server.js",
    "build": "npm install && echo 'Vercel Node.js API server built successfully' && ls -la",
    "start": "node server.js"
  },
  "keywords": ["password", "sync", "vercel", "mongodb", "api"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^4.17.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@vercel/node": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}